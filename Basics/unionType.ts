/* Union types let a variable hold more than one type.
Use | (pipe) symbol to define multiple allowed types.
 */

/*
Create a variable userId that can be a number or string.

Assign a number to it and print it.

Then assign a string to it and print again.
 */

let userId : number | string ;

userId = 17;
userId = "JD1708"

console.log(userId);

 